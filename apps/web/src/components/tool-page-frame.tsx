import Link from "next/link";
import type { Dictionary } from "@sfrankey/i18n";
import type { Locale, ToolDefinition } from "@sfrankey/shared";
import { toolDefinitions } from "@sfrankey/shared";
import { Badge, Card } from "@sfrankey/ui";
import { localePath } from "@/lib/locale";
import { ToolActions } from "@/components/tool-actions";
export function ToolPageFrame({ locale, tool, dictionary, children }: { locale: Locale; tool: ToolDefinition; dictionary: Dictionary; children: React.ReactNode }) { const related = toolDefinitions.filter((item) => item.category === tool.category && item.id !== tool.id).slice(0, 3); return <div className="mx-auto max-w-6xl px-4 py-10 sm:px-6"><div className="max-w-3xl"><div className="flex flex-wrap items-center gap-3"><Badge>{dictionary.categories[tool.category]}</Badge><span className="text-xs font-semibold text-emerald-600">● {dictionary.common.onDevice}</span></div><h1 className="mt-4 text-4xl font-black tracking-tight sm:text-5xl">{tool.title[locale]}</h1><p className="mt-4 text-lg leading-8 text-slate-600 dark:text-slate-300">{tool.description[locale]}</p><div className="mt-5"><ToolActions locale={locale} toolId={tool.id} /></div></div><div className="mt-8">{children}</div><div className="mt-12 grid gap-6 lg:grid-cols-2"><Card><h2 className="text-lg font-bold">{dictionary.tool.guide}</h2><p className="mt-3 text-sm leading-7 text-slate-600 dark:text-slate-300">{locale === "vi" ? "Nhập dữ liệu vào khu vực công cụ, kiểm tra kết quả và dùng nút sao chép hoặc tải xuống. Dữ liệu của công cụ này được xử lý trực tiếp trong trình duyệt." : "Enter data in the tool workspace, verify the result and use copy or download. This tool processes its data directly in your browser."}</p></Card><Card><h2 className="text-lg font-bold">{dictionary.tool.privacy}</h2><p className="mt-3 text-sm leading-7 text-slate-600 dark:text-slate-300">{locale === "vi" ? "Không gửi dữ liệu công cụ lên server, không đưa dữ liệu vào URL và không lưu nội dung nhạy cảm vào localStorage." : "Tool data is not uploaded to the server, added to the URL or stored in localStorage."}</p></Card></div>{related.length ? <section className="mt-12"><h2 className="text-xl font-bold">{dictionary.tool.related}</h2><div className="mt-4 grid gap-4 sm:grid-cols-3">{related.map((item) => <Link href={localePath(locale, `tools/${item.slug}`)} key={item.id}><Card className="h-full hover:border-brand-300"><span className="text-xl">{item.icon}</span><h3 className="mt-3 font-bold">{item.title[locale]}</h3><p className="mt-1 text-sm text-slate-500">{item.description[locale]}</p></Card></Link>)}</div></section> : null}</div>; }
