import Link from "next/link";
import { getDictionary } from "@sfrankey/i18n";
import { toolDefinitions, categories, type Locale } from "@sfrankey/shared";
import { localePath } from "@/lib/locale";
import { Badge, Card } from "@sfrankey/ui";
export default async function ToolsPage({ params }: { params: Promise<{ locale: Locale }> }) { const { locale } = await params; const t = getDictionary(locale); return <div className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><Badge>{t.nav.tools}</Badge><h1 className="mt-4 text-4xl font-black tracking-tight">{t.nav.tools}</h1><p className="mt-3 max-w-2xl text-slate-500">{t.home.description}</p><div className="mt-10 space-y-10">{categories.map((category) => <section key={category}><div className="flex items-center justify-between"><h2 className="text-xl font-bold">{t.categories[category]}</h2><Link href={localePath(locale, `categories/${category}`)} className="text-sm font-semibold text-brand-600">View category →</Link></div><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{toolDefinitions.filter((tool) => tool.category === category).map((tool) => <Link key={tool.id} href={localePath(locale, `tools/${tool.slug}`)}><Card className="h-full transition hover:border-brand-300"><div className="flex gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-lg bg-brand-50 dark:bg-brand-950">{tool.icon}</span><div><h3 className="font-bold">{tool.title[locale]}</h3><p className="mt-1 text-sm text-slate-500">{tool.description[locale]}</p></div></div></Card></Link>)}</div></section>)}</div></div>; }
