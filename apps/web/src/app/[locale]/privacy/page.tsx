import type { Locale } from "@sfrankey/shared";
import { getDictionary } from "@sfrankey/i18n";
import { Card } from "@sfrankey/ui";
import { ClearLocalData } from "@/components/clear-local-data";
export default async function PrivacyPage({ params }: { params: Promise<{ locale: Locale }> }) { const { locale } = await params; const t = getDictionary(locale); return <div className="mx-auto max-w-4xl px-4 py-12 sm:px-6"><h1 className="text-4xl font-black">{t.nav.privacy}</h1><p className="mt-4 max-w-2xl text-slate-500">{locale === "vi" ? "SFranKey ưu tiên xử lý dữ liệu trên thiết bị. Các công cụ local-first không gửi nội dung bạn nhập lên server." : "SFranKey prioritizes on-device processing. Local-first tools do not send your input to our server."}</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><Card><h2 className="font-bold">{locale === "vi" ? "Dữ liệu không rời trình duyệt" : "Data that stays in your browser"}</h2><p className="mt-2 text-sm leading-7 text-slate-500">TOTP secrets, passwords, JWTs, QR images, files, hash inputs and JSON are processed locally. They are not placed in URLs, analytics or local storage.</p></Card><Card><h2 className="font-bold">{locale === "vi" ? "Dữ liệu có thể gửi đi" : "Data that can leave the browser"}</h2><p className="mt-2 text-sm leading-7 text-slate-500">The optional time endpoint receives only a timestamp request. Feedback forms send only the fields you explicitly submit.</p></Card></div><div className="mt-8"><h2 className="text-lg font-bold">{locale === "vi" ? "Xóa dữ liệu cục bộ" : "Clear local data"}</h2><p className="mt-2 mb-4 text-sm text-slate-500">{locale === "vi" ? "Xóa theme, ngôn ngữ, yêu thích và lịch sử công cụ trên thiết bị này." : "Clear theme, language, favorites and recent tools from this device."}</p><ClearLocalData label={t.common.clear} /></div></div>; }
